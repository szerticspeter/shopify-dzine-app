#!/bin/bash

# Standalone script to run the product template creator app
# This can be used directly from the command line

cd "$(dirname "$0")"
echo "Starting Product Template Creator App..."
npm install
npm start